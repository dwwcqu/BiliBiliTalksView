# 基础采集页面本地联调计划

## 已确认范围与验收

用户已确认继续实际数据库升级与真实采集联调。沿用 feat/discussion-cache，只验证调试剧集 ep4292435 的页面 → HTTP → worker → PostgreSQL 流程。讨论浏览、模型分析和公网部署不在范围内。源端缺失允许 partial，但必须明确展示且不覆盖已有可读结果。

## 操作设计

使用现有本地开发库 bilibili_talks；先确认无活动写入，创建完整备份，并在独立临时恢复库核验记录数量与内容指纹。先对恢复库演练升级，再升级原库。备份、配置及运行证据仅放忽略的 data/verification/。不修改 D:/Data 下游导出。

网站仅监听 127.0.0.1，使用稳定的本地 RATE_LIMIT_SECRET；Cookie 仅通过服务器文件配置。保留现有来源控制数据库，不绕过暂停或冷却。通过浏览器提交已知调试链接，验证缓存读取；明确触发一次手动刷新，worker 执行有限请求。出现来源拒绝时保留暂停与诊断，停止额外来源请求；成功结果核对数据库与页面一致。运行进程在结束时停止或明确交接。

## 任务

- [x] 核对实际版本、配置、进程和网站静态入口，修复阻碍运行的缺陷并测试。
- [x] 备份、独立恢复、升级演练、内容一致性核验，再升级开发库。
- [x] 启动本地 API/worker，浏览器验证缓存读取和一次真实刷新。
- [x] 验证入库结果、重复提交与只读页面恢复，记录未完成的来源约束。
- [x] 同步使用说明与阶段状态，完成审阅和测试；验收通过后按既定流程提交、合并并推送 main。

## 回退

任何恢复/演练失败均不升级原库。迁移失败保持停写并检查事务回滚；确需备份恢复时先保留失败库，不自动覆盖原数据。真实来源受限不视为代码全流程成功。

## 已落实的运行决定

开发库已从 0001 升级到 0005，完整备份和独立恢复库保留在本地；恢复、演练和正式迁移均核对全部评论及关联元数据内容。运行入口路径经核对正确，无需改代码。页面首次访问已有导入缓存，只有用户手动更新才创建本次真实任务；导入数据缺少可信完整刷新上下文时，auto 按既定规则选择 full。

## 验收结论与运行交接

真实任务已结束并原子入库，页面终态、评论/楼数、部分覆盖及上下文缺口提示与数据库一致。原有评论 ID 全部保留，旧正文复用；普通提交返回缓存，活动任务内及跨小时重复刷新均复用同一任务，页面恢复和重载没有写请求。

主楼清单和楼中楼处理均已结束，但源端不可得的旧楼与未重新观察到的旧评论保守保留，因此结果为 partial；此状态是已完成的部分覆盖结果，不是采集仍在运行，也不能宣称不可见内容已获取。后续自动模式依据已保存的完整范围核验证据选择增量，普通读取不触发刷新。

本地 API 仅监听 127.0.0.1:8000，常驻 worker 用于处理后续显式提交；未部署公网。备份和恢复演练库保留，D:/Data 下游导出未修改。讨论浏览与模型分析仍留待单独确认和设计。
